# Machine Learning & Image Processing (Pure TensorFlow)
numpy>=1.23.0
Pillow>=9.0.0

# TensorFlow Lite Runtime for Raspberry Pi (or install full tensorflow)
# On Raspberry Pi: pip install tflite-runtime
tflite-runtime>=2.14.0; platform_machine == "armv7l" or platform_machine == "aarch64"
tensorflow>=2.12.0; platform_machine != "armv7l" and platform_machine != "aarch64"

# Raspberry Pi Hardware Drivers (Installed on Raspberry Pi OS / Linux)
# RPi.GPIO>=0.7.1; platform_system == "Linux"
# picamera2; platform_system == "Linux"
